# constraints.txt — version floors for transitive dependencies
# Use: pip install -r requirements.txt -c constraints.txt
#
# Pillow>=11.2.1 is required for Python 3.14 (pre-built arm64 wheel).
# gradio==6.17.3 allows Pillow<13, so Pillow 12.x satisfies both.
# sentence-transformers has no Pillow floor, so this constraint pins it safely.

Pillow>=11.2.1